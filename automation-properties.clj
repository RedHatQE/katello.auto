;; Example configuration.  Edit this file and copy it to your home
;; directory, where it will be read when automation starts.
;; You should specify at least the :server-url.
;;
;; For other options and defaults, see src/katello/conf.clj

{
 :server-url "https://my.server.hostname.com/katello/"
}
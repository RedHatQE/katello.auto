;; Example configuration. Copy this file to automation-properties.clj
;; in your project directory. Edit the copy to reflect your own
;; environment. When automation starts it will read the file. You
;; should specify at least the :server-url.
;;
;; For other options and defaults, see katello.conf/defaults

{
 :server-url "https://my.server.hostname.com/katello/"
}